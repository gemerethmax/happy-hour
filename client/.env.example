# API Base URL
# Local development (if server is on different port): http://localhost:3000/api
# Production (same domain): /api (or leave unset, defaults to /api)
VITE_API_URL=http://localhost:3000/api
